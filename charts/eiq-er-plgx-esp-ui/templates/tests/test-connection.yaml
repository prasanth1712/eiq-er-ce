apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eiq-er-plgx-esp-ui.fullname" . }}-test-connection"
  labels:
    {{- include "eiq-er-plgx-esp-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eiq-er-plgx-esp-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
