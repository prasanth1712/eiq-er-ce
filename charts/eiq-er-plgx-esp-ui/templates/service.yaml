apiVersion: v1
kind: Service
metadata:
  name: {{ include "eiq-er-plgx-esp-ui.fullname" . }}
  labels:
    {{- include "eiq-er-plgx-esp-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eiq-er-plgx-esp-ui.selectorLabels" . | nindent 4 }}
