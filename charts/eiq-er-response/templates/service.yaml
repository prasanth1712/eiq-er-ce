apiVersion: v1
kind: Service
metadata:
  name: {{ include "eiq-er-response.fullname" . }}
  labels:
    {{- include "eiq-er-response.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eiq-er-response.selectorLabels" . | nindent 4 }}
