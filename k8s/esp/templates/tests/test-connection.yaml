apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "esp.fullname" . }}-test-connection"
  labels:
    {{- include "esp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "esp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
