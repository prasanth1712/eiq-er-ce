apiVersion: v1
kind: Service
metadata:
  name: {{ include "esp.fullname" . }}
  labels:
    {{- include "esp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "esp.selectorLabels" . | nindent 4 }}
